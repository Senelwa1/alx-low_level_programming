#include "main.h"

/**
 * mul - Mutiplies two integers
 * @x: The first number to multiply
 * @y: The second number to mutiply with first number
 * Return: The result of mutiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
