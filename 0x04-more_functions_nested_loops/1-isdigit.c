#include "main.h"

/**
 * _isdigit - Check if acharacter is a digit
 * 0x: The number to be checked
 * Return: 1 for character that will be a digit 0r 0 for any else
 */

int _isdigit(int x)

{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
	
	
